import { PrefixTextPipe } from './prefix-text.pipe';

describe('PrefixTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PrefixTextPipe();
    expect(pipe).toBeTruthy();
  });
});
